version: 2.1

jobs:
  build:
    docker:
      - image: alpine:3.15
        auth:
          username: user
          password: azerty
    steps:
      - run:
          name: Build docker image
          command: |
            apk add docker
            addgroup user docker
            docker build . -t bog13/lettings
  test:
    docker:
      - image: alpine:3.15
    steps:
      - run: 
          name: Test the app
          command: echo "=> Test"
  deploy:
    docker:
      - image: alpine:3.15
    steps:
      - run:
          name: Deploy to AWS
          command: echo "=> Deploy"

workflows:
  build_test_and_deploy:
    jobs:
      - build
      - test:
          requires:
            - build
      - deploy:
          requires:
            - test

